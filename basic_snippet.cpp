#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define vi vector<int>
#define vin(x, v)     \
    for (auto &x : v) \
        cin >> x;
#define vout(x, v)   \
    for (auto x : v) \
        cout << x << " ";


// maps < unordered maps


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    set<int>st;
    for(auto x:st){

    }
}